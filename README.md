This project demonstrates how to use custom exceptions in Java.

It includes:
- A custom exception class (InvalidRadiusException)
- A Circle class with validation
- A test class to demonstrate exception handling

The program throws an exception when an invalid radius is used.
